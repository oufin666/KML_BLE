AR_Glasses-main 为硬件代码，支持接收大容量kml压缩文件后解压缩并流式写入sd卡；
kml_compare 是一个用于对比两个kml文件是否一致的python脚本，用于校验解压缩结果与原文件是否一致，可以找出两文件不同之处的数目和位置，便于硬件代码纠错；
miniprogram-1 是小程序代码，用于发送kml文件并压缩、切片，内含解压缩测试功能，用于校验压缩以及解压缩算法的正确性；
miniprogram-2 是小程序代码，用于发送kml文件并转换为包含关键坐标信息的二进制文件。
